const callModule = require('./callModule');
const emailModule = require('./emailModule');

// Start the calling process
callModule.startCalling();

// Start the emailing process
emailModule.startEmailing();
